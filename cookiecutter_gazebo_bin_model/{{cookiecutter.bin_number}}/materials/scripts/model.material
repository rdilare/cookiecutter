material {{cookiecutter.bin_number}}
{
  technique
  {
    pass
    {
      texture_unit
      {
        // Relative to the location of the material script
        texture ../textures/texture_({{cookiecutter.idx}}).png
        // Repeat the texture over the surface (4 per face)
        scale 1 1
      }
    }
  }
}
